# Dependency boundary: exact versions, hardcoded and spellchecked (Clip 2, slide 4).
# boto3 is provided by the Lambda base image but pinned here so the SBOM and SCA
# see the full resolved tree.
boto3==1.34.144
requests==2.32.3
